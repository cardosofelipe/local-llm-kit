# Local-LLM-Kit Configuration
# https://github.com/yourusername/local-llm-kit
#
# Copy this file to .env and customize for your environment
# cp .env.example .env

# Docker image tags
OLLAMA_DOCKER_TAG=latest
WEBUI_DOCKER_TAG=main

# Storage paths - customize based on your partition layout
# Default: Uses relative paths in project directory for portability
OLLAMA_MODELS_PATH=./data/models/ollama
OLLAMA_CONFIG_PATH=./config/ollama
OPENWEBUI_DATA_PATH=./data/open-webui

# Alternative storage configurations (uncomment and modify as needed):
# For systems with separate fast storage:
# OLLAMA_MODELS_PATH=/data/ollama-models
# OLLAMA_MODELS_PATH=/storage/ai-ml/ollama/models
# OLLAMA_MODELS_PATH=/workdrive/data/models/ollama
#
# For better backup integration:
# OPENWEBUI_DATA_PATH=/storage/ai-ml/open-webui/data

# Network configuration
# Using 11xxx range to avoid conflicts with common dev servers
OPEN_WEBUI_PORT=11300
SEARXNG_PORT=11380

# Security - IMPORTANT: Generate a proper secret key
# Run: openssl rand -hex 32
WEBUI_SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL

# Optional: Authentication settings
ENABLE_SIGNUP=true
DEFAULT_USER_ROLE=user

# Default Admin Account (for automated setup)
# These credentials are used to create the first admin user automatically
# during initial setup. CHANGE THE PASSWORD after first login!
WEBUI_ADMIN_NAME=admin
WEBUI_ADMIN_EMAIL=admin@localhost
WEBUI_ADMIN_PASSWORD=admin123

# Ollama Performance Tuning
# These values are set by setup.sh based on your hardware
# You can adjust them manually if needed
#
# Performance Tiers:
#   Low (CPU-only):     OLLAMA_NUM_PARALLEL=1, OLLAMA_MAX_LOADED_MODELS=1
#   Medium (Balanced):  OLLAMA_NUM_PARALLEL=2, OLLAMA_MAX_LOADED_MODELS=1
#   High (GPU systems): OLLAMA_NUM_PARALLEL=4, OLLAMA_MAX_LOADED_MODELS=2
OLLAMA_NUM_PARALLEL=2
OLLAMA_MAX_LOADED_MODELS=1

# AMD GPU Configuration
# For RDNA 3.5 (gfx1151) like Radeon 8060S, use 11.0.0 for compatibility
# Alternatives to try if you have issues: 11.5.1, 10.3.0
HSA_OVERRIDE_GFX_VERSION=11.0.0

# OpenWebUI Performance tuning
ENABLE_RAG_WEB_SEARCH=true
WEBUI_LOG_LEVEL=info

# SearXNG Configuration
SEARXNG_CONFIG_PATH=./config/searxng
SEARXNG_BASE_URL=http://searxng:8080
# Generate secret key with: openssl rand -hex 32
SEARXNG_SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL

# Web Search Settings (PersistentConfig - saved to DB on first boot)
RAG_WEB_SEARCH_ENGINE=searxng
RAG_WEB_SEARCH_RESULT_COUNT=5
RAG_WEB_SEARCH_CONCURRENT_REQUESTS=10
